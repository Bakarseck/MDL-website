+ [  ] Linking Different pages html
    + [ x ] Linking the `index` page
    + [ x ] Linking the `about` page
    + [  ] Linking the `authors` page
    + [  ] Linking the `book` page
    + [  ] Linking the `citations` page
    + [  ] Linking the `conditions` page
    + [  ] Linking the `faq` page
    + [  ] Linking the `politiques` page
+ [  ] Stylize different pages
    + [  ] Stylize the `header` and the `footer`